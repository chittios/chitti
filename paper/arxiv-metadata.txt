arXiv submission metadata
=========================
GENERATED by `make arxiv-meta` from main.tex -- do not edit by hand, or the form
and the paper will drift. Everything below is ASCII and within arXiv's limits.

TITLE (96 chars)
-----
The Determinism Boundary: An Operating-System Capability ABI for Agents as the Unit of Execution

  Checked: not all-caps, no Unicode, no cryptic macros left to expand.

ABSTRACT (1913 chars; arXiv rejects over 1920)
--------
For fifty years the operating system's unit of execution has been the program: untrusted, but deterministic, with knowable effects. An LLM agent breaks all three. Its instruction stream is stochastic, its request set unbounded, and its apparent intent derives from data it has read -- so whoever controls that data controls the requests. Practice answers above the OS, with prompt guardrails and per-call approval, while the agent holds the user's ambient authority. We argue the answer belongs in the kernel, and present Synapse, the capability ABI of ChittiOS, an OS whose unit of execution is an agent, not a binary. Synapse draws an explicit determinism boundary: above it a stochastic planner emits an untrusted plan; below it deterministic native code executes capability-checked primitives. Model output never causes an effect. A fixed registry generates a prefix-closed grammar, applied in the model's own token space and revalidated at the executor; capabilities attenuate on delegation and a scope gate binds each to concrete paths; and a provenance lattice makes taint a syscall argument, refusing destructive primitives justified by untrusted content -- Biba integrity over the live context. Every attempt, denials included, is audited. The model runs in-kernel with no I/O of its own, so there is no way around the ABI; the mechanism is 3.6k lines of a dual-architecture Rust kernel. Measured: the authorization decision costs 1.05 us ($2.4\times10^{-5}$ of one decoded token), and an injection corpus is refused 13 times out of 13 -- while removing provenance lets 10 through, removing attenuation all 13, and a human declassifying the attacker's source 6. The cost is a quarter of the legitimate irreversible operations in a benign suite, and the obvious cheap fix makes it worse: a syntactic per-value relation permits 4 attacks and recovers none, because the model, not the code, is the dataflow.

  Checked: ASCII only; no ~ \, or font commands; no URLs (arXiv does not
  linkify them in abstracts); em dashes written as `--`. Derived from the LaTeX
  abstract, so the form and the PDF cannot disagree.

CATEGORIES
----------
  Primary:    cs.OS  (Operating Systems)
  Cross-list: cs.CR  (Cryptography and Security)
              cs.AI  (Artificial Intelligence)

  cs.OS is primary because the contribution is a syscall-layer mechanism, not a
  model or an attack. cs.CR is where the injection-defence audience reads.

LICENSE
-------
  Recommended: CC BY 4.0 -- it matches the artifact's Apache-2.0 posture and
  permits the reuse an artifact paper wants. arXiv's default minimal licence
  also works but blocks redistribution. The author's call, and arXiv cannot
  change it after announcement.

BEFORE YOU UPLOAD
-----------------
  1. make arxiv                 -> builds and checks arxiv-submission.tar.gz
  2. Make the repo public (or drop the artifact citation) -- see README.
  3. Tag the release and name the tag in section 4 and refs.bib.
  4. arXiv rebuilds the PDF from source: check THEIR output, not yours, before
     clicking submit. It is a required step and the last chance to catch a
     figure that moved.
  5. New submitters may need an endorsement for cs.OS.
